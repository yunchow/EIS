<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eis.base.domain.repository.OrganizationRepository">
	
	<resultMap type="hashmap" id="menuMap">
		<id property="id"/>
		<id property="name" column="name"/>
		<id property="text" column="name"/>
	</resultMap>
	
	<select id="findAll" resultMap="menuMap">
		select * from sys_organization
		order by seq
	</select>
	
	<select id="findById" parameterType="String" resultMap="menuMap">
		select * from sys_organization
		where id = #{id}
		order by seq
	</select>
	
	<insert id="add" parameterType="com.eis.base.domain.entity.Menu">
		insert into sys_organization 
			(id, name, oganizer, pid, comment) 
		values 
		    (#{id}, #{name}, #{oganizer}, #{pid}, #{comment})
	</insert>
	
	<update id="update" parameterType="com.eis.base.domain.entity.Menu">
		update sys_organization 
		    set name = #{name}, oganizer = #{oganizer}, pid = #{pid}, comment = #{comment} 
		where 
		    id = #{id}
	</update>
	
	<delete id="delete" parameterType="String">
		delete from sys_organization where id=#{id} or pid=#{id}
	</delete>

</mapper>