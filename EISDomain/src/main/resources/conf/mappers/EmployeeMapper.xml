<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eis.domain.rep.EmployeeRep">
	<select id="findAll" resultType="Employee">
		select * from employee
	</select>
	
	<select id="findById" resultType="Employee">
		select * from employee where employeeId=#{employeeId}
	</select>
	
	<insert id="add" parameterType="Employee">
		insert into employee(no, name, gender) values(#{no}, #{name}, #{gender})
	</insert>
	
	<update id="update" parameterType="Employee">
		update employee set name=#{name}, gender=#{gender} where employeeId=#{employeeId}
	</update>
	
	<delete id="delete" parameterType="int">
		delete from employee where employeeId=#{employeeId}
	</delete>

</mapper>